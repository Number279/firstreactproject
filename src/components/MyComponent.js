import React from 'react';

//you should almost always use const to declare variables
const MyComponent = () => <div>This can immediately return html from a function!</div>

//you can return html as the result of a function in React, sweet!
export default MyComponent;
//export allows you to use the component anywhere else with import